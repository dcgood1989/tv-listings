<%= link_to "Edit", edit_show_path %>
<%= link_to "New Show Listing", new_show_listing_path %>

<% if @show.show_listings.empty? %>
  <%= "No Show Listings for #{@show.name}" %>
<% else  %>
  <%= "Revenge of the Semicolon is showing on Channel 12 on 11/11/2011" %>

  <% end %>
